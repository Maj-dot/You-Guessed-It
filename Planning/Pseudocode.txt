// 1) Define the required variables
let wordToGuess;
let spaceMan = []; // Guessed letters
let incorrectGuesses = 0;
let maxGuesses = 6; // Maximum incorrect guesses
let gameOver = false;

// 2) Cache DOM elements
const wordDisplayArea = document.getElementById('wordDisplayArea');
const keyBoardButton = document.getElementById('keyBoardButton');
const scoreDisplay = document.getElementById('scoreDisplay');
const resetButton = document.getElementById('resetButton');

// 3) Initialize the game
function initializeGame() {
    wordToGuess = getRandomWord(); // Function to get a random word
    spaceMan = [];
    incorrectGuesses = 0;
    gameOver = false;
    renderGameState();
}

// 4) Render the game state
function renderGameState() {
    // Display the word with blanks for unguessed letters
    // Update guessed letters and incorrect guesses
    // Display the score and game over message if applicable
}

// 5) Handle letter guesses
function handleClick(event) {
    if (!gameOver) {
        let guessedLetter = event.target.value.toUpperCase();
        if (!spaceMan.includes(guessedLetter)) {
            spaceMan.push(guessedLetter);
            if (wordToGuess.includes(guessedLetter)) {
                // Correct guess, update word display
            } else {
                incorrectGuesses++;
                if (incorrectGuesses >= maxGuesses) {
                    gameOver = true;
                    // Display game over message
                }
            }
        }
        renderGameState();
    }
}

// 6) Reset the game
function resetGame() {
    initializeGame();
}
